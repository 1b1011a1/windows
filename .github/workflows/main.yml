name: CI

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: 下载
      run: Invoke-WebRequest https://www.cpolar.com/static/downloads/releases/3.3.18/cpolar-stable-windows-amd64-setup.zip?_gl=1*r86rhh*_ga*NDAyNTQ2NDUyLjE3MzY1NjAwMTk.*_ga_WF16DPKZZ1*MTczNjU2MDAxOC4xLjEuMTczNjU2MDA5OS41MC4wLjA. -OutFile cpolar.zip
    - name: 解压
      run: Expand-Archive cpolar.zip
    - name: 安装
      run: cmd /c "msiexec /i .\cpolar\cpolar_amd64.msi /qn"
    - name: 登录
      run: '& "C:\Program Files\cpolar\cpolar.exe" authtoken  $Env:CPLAR_TOKEN'
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.CPLAR_TOKEN }}
    - name: 启用远程桌面
      run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server'-name "fDenyTSConnections" -Value 0
    - run: Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
    - run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 1
    - run: Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "P@ssw0rd!" -Force)
    - name: 穿透3389端口
      run: '& "C:\Program Files\cpolar\cpolar.exe" tcp 3389'
